/* ==== 以下为0.4新增内容移植 ==== */
html {
    font-size: 14px;
}

@media (min-width: 768px) {
    html {
        font-size: 16px;
    }
}

.btn:focus, .btn:active:focus, .btn-link.nav-link:focus, .form-control:focus, .form-check-input:focus {
    box-shadow: 0 0 0 0.1rem white, 0 0 0 0.25rem #258cfb;
}

html {
    position: relative;
    min-height: 100%;
}

body {
    margin-bottom: 60px;
}

.author-slider {
    position: relative;
}

    .author-slider .btn-slider {
        position: absolute;
        top: 50%;
        transform: translateY(-50%);
        z-index: 10;
    }

    .author-slider .btn-prev {
        left: 0;
    }

    .author-slider .btn-next {
        right: 0;
    }
/* 自定义背景与Google风格居中布局 */
body.custom-bg {
    background: url('/img/background.jpg') no-repeat center center fixed;
    background-size: cover;
    min-height: 100vh;
}

/* Google主界面居中 */
.google-center {
    min-height: 65vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
}

.input-group-lg .form-control,
.input-group-lg .input-group-text {
    border-radius: 2rem;
    font-size: 1.25rem;
    background: #fff;
}

/* Google风格的主搜索框form */
.google-center form {
    border-radius: 2rem;
    padding: 2rem 1rem;
    max-width: 500px;
    margin: 0 auto;
}

.google-center img {
    filter: drop-shadow(0 4px 16px rgba(0,0,0,0.12));
}

/* Google风格主搜索框（已不用可不加） */
.jbook-main-search {
    display: flex;
    justify-content: center;
    align-items: center;
    margin-top: 60px;
    margin-bottom: 30px;
}

    .jbook-main-search input[type="text"] {
        width: 480px;
        height: 48px;
        padding: 0 22px;
        border: none;
        border-radius: 30px;
        background: #fff;
        box-shadow: 0 2px 10px 0 rgba(60,64,67,.15), 0 1.5px 6px 0 rgba(60,64,67,.11);
        font-size: 1.25rem;
        outline: none;
        transition: box-shadow .2s;
    }

        .jbook-main-search input[type="text"]:focus {
            box-shadow: 0 4px 16px 0 rgba(60,64,67,.20), 0 2px 8px 0 rgba(60,64,67,.13);
            background: #fff;
        }

/* 搜索条相关样式 */
.search-container {
    display: flex;
    justify-content: center;
    align-items: center;
    margin-top: 48px;
}

.search-bar-form {
    position: relative;
    width: 600px;
    max-width: 96vw;
    background: #fff;
    border-radius: 24px;
    box-shadow: 0 2px 8px rgba(60,64,67,.15);
    display: flex;
    align-items: center;
    height: 48px;
    padding: 0;
}

.search-bar-input {
    border: none;
    outline: none;
    width: 100%;
    font-size: 1.1rem;
    padding: 0 60px 0 18px; /* 右侧padding给大图标留空间 */
    border-radius: 24px;
    background: transparent;
    height: 48px;
    line-height: 48px;
    color: #333;
    box-sizing: border-box;
}

.search-bar-btn {
    background: none;
    border: none;
    outline: none;
    position: absolute;
    right: 14px; /* 靠近右边 */
    top: 50%;
    transform: translateY(-50%);
    padding: 0;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
}

.search-bar-icon {
    width: 32px; /* 增大为32px或更大 */
    height: 32px;
    opacity: 0.8;
    transition: opacity 0.2s;
}

.search-bar-btn:hover .search-bar-icon {
    opacity: 1;
}

/* 页脚与页脚内链接 */
footer, footer .container {
    color: #fff !important;
}

    footer a {
        color: #0d6efd !important; /* Bootstrap蓝色 */
        text-decoration: underline;
    }

        footer a:hover {
            color: #0056b3 !important; /* 深蓝，hover效果 */
        }

/* 导航栏透明、毛玻璃、白色字体与高亮 */
.navbar,
.navbar.navbar-expand-sm {
    background: rgba(30, 30, 40, 0.30) !important;
    backdrop-filter: blur(12px) !important;
    -webkit-backdrop-filter: blur(12px) !important;
    box-shadow: none !important;
    border: none !important;
}

    .navbar .navbar-brand,
    .navbar .navbar-nav .nav-link,
    .navbar .navbar-nav .nav-item,
    .navbar .navbar-text {
        color: #fff !important;
        font-weight: 500;
        text-shadow: 0 1px 6px rgba(0,0,0,0.15);
    }

    .navbar .nav-link:focus,
    .navbar .nav-link:active,
    .navbar .nav-link:visited,
    .navbar .nav-link:focus-visible {
        outline: none !important;
        box-shadow: none !important;
        border: none !important;
        text-decoration: none !important;
    }

    .navbar .navbar-nav .nav-link:hover,
    .navbar .navbar-nav .nav-item:hover,
    .navbar .navbar-brand:hover {
        background: rgba(255,255,255,0.18) !important;
        backdrop-filter: blur(24px) !important;
        -webkit-backdrop-filter: blur(24px) !important;
        color: #ffe658 !important;
        border-radius: 8px;
        transition: background 0.2s, color 0.2s, backdrop-filter 0.2s;
    }

    .navbar .navbar-nav .nav-link,
    .navbar .navbar-brand {
        padding: 8px 18px !important;
        margin: 0 2px;
        border-radius: 8px;
    }

    .navbar a, .navbar a:hover, .navbar a:focus {
        text-decoration: none !important;
    }

/* Privacy页面内容区域虚化、白底、黑字 */
.container.mt-4 {
    background: rgba(255,255,255,0.68); /* 半透明白色 */
    box-shadow: 0 4px 24px rgba(0,0,0,0.11);
    border-radius: 24px;
    padding: 44px 32px 36px 32px;
    margin-top: 48px;
    margin-bottom: 48px;
    backdrop-filter: blur(14px) !important;
    -webkit-backdrop-filter: blur(14px) !important;
    color: #161616 !important;
    font-weight: 500;
}

    .container.mt-4 h2,
    .container.mt-4 h4 {
        color: #101010 !important;
        font-weight: 700;
    }
/* ==== 0.4自定义CSS移植结束 ==== */


/* Share 表单内容外层容器：虚化+白色半透明 */
.share-content-blur {
    background: rgba(255,255,255,0.68);
    box-shadow: 0 4px 24px rgba(0,0,0,0.11);
    border-radius: 24px;
    max-width: 640px;
    margin: 40px auto;
    padding: 36px 42px 36px 42px;
    margin-top: 48px;
    margin-bottom: 48px;
    backdrop-filter: blur(14px);
    -webkit-backdrop-filter: blur(14px);
    color: #161616 !important;
    font-weight: 500;
    /* 水平居中 */
    margin-left: auto;
    margin-right: auto;
    flex-direction: column;
    align-items: center;
    display: flex;
}

.col-form-label {
    font-size: 1.2rem; /* 标签大些 */
    font-weight: bold; /* 标签加粗 */
}

h2, .form-section-title, .container.mt-4 h4 {
    font-size: 2rem;
    font-weight: bold;
    letter-spacing: 1px;
}


.share-content-blur {
    max-width: 560px; /* 关键：变窄！如还宽可继续缩小 */
    margin: 48px auto 0 auto;
    padding: 40px 38px 34px 38px;
    border-radius: 36px;
    background: rgba(255,255,255,0.72);
    box-shadow: 0 4px 32px rgba(0,0,0,0.14);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
}

    .share-content-blur h2 {
        font-size: 2.6rem;
        font-weight: 700;
        text-align: center;
        margin-bottom: 32px;
        letter-spacing: 1px;
    }

    /* 保证表单不会溢出 */
    .share-content-blur form {
        width: 100%;
    }

.custom-submit-btn {
    width: 66%; /* 控制宽度，可以调50%~70%，自己感觉合适就行 */
    max-width: 400px; /* 最大宽度，防止太宽 */
    min-width: 160px; /* 最小宽度，手机上也不至于太窄 */
    margin: 0 auto;
    margin-top: 36px;
    display: block;
    padding: 14px 0;
    font-size: 1.55rem;
    font-weight: 700;
    color: #fff;
    background: linear-gradient(90deg, #20d06e 0%, #43e97b 100%);
    border: none;
    border-radius: 32px;
    box-shadow: 0 2px 12px rgba(67, 233, 123, 0.10);
    transition: background 0.2s, box-shadow 0.2s;
}

    .custom-submit-btn:hover {
        background: linear-gradient(90deg, #43e97b 0%, #38f9d7 100%);
        box-shadow: 0 4px 20px rgba(67, 233, 123, 0.20);
    }


    .custom-submit-btn:hover, .custom-submit-btn:focus {
        background: #27ae60 !important;
    }
